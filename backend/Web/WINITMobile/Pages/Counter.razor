@page "/counter"

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>
<button class="btn btn-primary" @onclick="CopySQLiteFile">Copy Sqlite</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
    public void CopySQLiteFile()
    {
        // string sourcePath = Path.Combine(wwwrootFolderPath, "SQLiteFile.db");
        // string destinationPath = Path.Combine(Environment.CurrentDirectory, "SQLiteFile.db");

        // if (!File.Exists(destinationPath))
        // {
        //     File.Copy(sourcePath, destinationPath);
        // }
    }
}
