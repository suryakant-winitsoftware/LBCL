// Export the common schedule customers component and types
export { 
  ScheduleCustomersManager,
  type Customer,
  type TimeSlot,
  type ScheduleConfig,
  type ScheduleCustomersManagerProps
} from './ScheduleCustomersManager';

// Re-export for convenience
export { ScheduleCustomersManager as ScheduleManager } from './ScheduleCustomersManager';